modules = ["php", "php-8.2"]

[nix]
channel = "stable-24_05"
packages = ["php"]

[workflows]

[deployment]
run = ["sh", "-c", "cp .env.example .env && composer install && php artisan key:generate && php artisan migrate --seed && php artisan serve --host=0.0.0.0 --port=5000"]
